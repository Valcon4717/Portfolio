import React from 'react';
// import styles from '@/styles/about.module.css';




const About = (props: { theme:any }) => {
    return (
      <div id="about" className={`${props.theme}`}>
        fdsoifjsdoifj
      </div>
    );
};

export default About;